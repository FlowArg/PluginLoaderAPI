archivesBaseName = "PluginTest"

dependencies {
    compileOnly parent
}


tasks.build.doLast {
    copy {
        from "$buildDir/libs/" + archivesBaseName + ".jar"
        into "$rootDir/plugins/"
    }
}