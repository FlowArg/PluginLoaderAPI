archivesBaseName = project.name + "PluginTest"

dependencies {
    compileOnly parent.project(':SampleImplementation')
    compileOnly parent
}

tasks.build.doLast {
    copy {
        from "$buildDir/libs/" + archivesBaseName + ".jar"
        into "$rootDir/plugins/another/"
    }
}